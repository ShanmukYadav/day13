# Day 14 | AM Take-Home — Pandas Series & DataFrames: Product Analytics

> **Submission path:** `/week3/day14/pandas_intro/`  
> **Due:** Next day 09:15 AM

---

## 📁 Project Structure

```
pandas_intro/
├── product_analyzer.py      # Part A — E-Commerce Product Analyzer
├── comparison_report.py     # Part B — Multi-DataFrame Comparison Report
├── interview_answers.py     # Part C — Interview-Ready Q&A + Debug
├── ai_augmented_task.py     # Part D — AI-Augmented Data Quality Report
│
├── budget_products.csv      # Generated by product_analyzer.py
├── premium_products.csv     # Generated by product_analyzer.py
├── popular_products.csv     # Generated by product_analyzer.py
└── monthly_summary.csv      # Generated by comparison_report.py
```

---

## ▶️ How to Run

```bash
# Install dependencies (one-time)
pip install pandas numpy

# Run each part
python product_analyzer.py
python comparison_report.py
python interview_answers.py
python ai_augmented_task.py
```

---

## 📝 Part A — `product_analyzer.py` (40%)

Builds a 22-product e-commerce DataFrame across 4 categories: **Electronics, Clothing, Books, Home**.

| Feature | Detail |
|---|---|
| Dataset | 22 products, 6 columns (`name`, `category`, `price`, `stock`, `rating`, `num_reviews`) |
| First 5 Minutes checklist | `.shape`, `.head()`, `.info()`, `.describe()`, `.isnull().sum()`, `.dtypes` |
| `.loc[]` ops | Filter by category; compound boolean condition; update a single cell safely |
| `.iloc[]` ops | First/last 5 rows; every other row; row-range with column-range |
| Filtered DataFrames | `budget_products` (price < 1000), `premium_products` (price > 10000), `popular_products` (reviews > 100 and rating > 4.0) |
| CSV export | All 3 filtered DataFrames exported via a `for` loop |

---

## 📝 Part B — `comparison_report.py` (30%)

Compares sales data across **January, February, and March** using multiple DataFrames.

| Feature | Detail |
|---|---|
| DataFrames | 3 months × 10 products each, generated with `np.random` for realistic variation |
| Per-month metrics | `total_revenue`, `avg_order_value`, `top_selling_product` |
| Summary DataFrame | Months as index, metrics as columns — exported to `monthly_summary.csv` |
| `.query()` | Filters by category + revenue threshold; filters by units_sold within a single month |
| `.nlargest()` / `.nsmallest()` | Top/bottom 3 products by revenue across all months; top 3 units sold in March |

**Self-study APIs used:** `pd.concat()`, `df.query()`, `df.nlargest()`, `df.nsmallest()`

---

## 📝 Part C — `interview_answers.py` (20%)

### Q1 — `.loc[]` vs `.iloc[]`

| | `.loc[]` | `.iloc[]` |
|---|---|---|
| Basis | **Label** (index values) | **Integer position** (0-based) |
| Slice end | **Inclusive** | **Exclusive** |
| Works with | Any index type | Always 0, 1, 2 … |

**Example with integer index `[0,1,2,3,4]`:**
- `df.loc[0:3]` → rows 0, 1, 2, **3** (inclusive)
- `df.iloc[0:3]` → rows 0, 1, **2** (exclusive)

**Example with string index `['a','b','c','d','e']`:**
- `df.loc['a':'d']` → `a`, `b`, `c`, **d** (inclusive)
- `df.iloc[0:3]` → positions 0, 1, 2 → `a`, `b`, **c**

---

### Q2 — `analyze_csv(filepath)` function

Returns a dict with:
- `num_rows`, `num_cols`
- `numeric_cols`, `categorical_cols`
- `null_counts` (per column)
- `memory_mb` (using `df.memory_usage(deep=True)`)

Also prints the full **First 5 Minutes checklist** to stdout.

---

### Q3 — Debugging (3 bugs found and fixed)

| Bug | Original (broken) | Fix |
|---|---|---|
| **Bug 1** | `df["age"] > 25 and df["salary"] > 55000` — Python `and` fails on Series | Use `&` with parentheses: `(df["age"] > 25) & (df["salary"] > 55000)` |
| **Bug 2** | `df["age"][0] = 26` — chained indexing silently fails | Use `.loc[0, "age"] = 26` for safe assignment |
| **Bug 3** | `df.iloc[0:2]` — comment says "3 rows" but gets 2 (exclusive end) | Use `.iloc[0:3]` to get rows 0, 1, 2 |

---

## 📝 Part D — `ai_augmented_task.py` (10%)

### Prompt used
> *"Write a Python function that takes a Pandas DataFrame and generates an automated data quality report including: shape, dtypes, missing values percentage, duplicate rows, unique value counts per column, and basic stats. Return the report as a dict and also print a formatted summary."*

### Tested on 3 DataFrames
1. **Clean DataFrame** — 5 products, no nulls, no duplicates
2. **Messy DataFrame** — nulls, duplicates, an all-null column, a single-value column
3. **Empty DataFrame** — edge case

### Improvements made to AI output

| Issue | Original AI | Fix Applied |
|---|---|---|
| Empty DataFrame crash | `ZeroDivisionError` on `len(df)` = 0 | Added `if df.empty` early return guard |
| Memory accuracy | `df.memory_usage().sum()` (shallow) | `deep=True` for accurate object column sizes |
| All-null columns | Not detected | Explicitly flagged as useless features |
| Single-value columns | Counted but not flagged | Flagged as zero-variance / useless for ML |
| Missing value % | Raw counts only | Added percentage alongside counts |
| Output formatting | Flat print dump | Sectioned summary with emoji markers + alignment |

---

## 🧠 Key Concepts Covered

- **Pandas Series vs DataFrame** — creation from dicts, list of dicts, NumPy arrays
- **`.loc[]`** — label-based, inclusive slicing, boolean mask filtering, safe assignment
- **`.iloc[]`** — position-based, exclusive slicing, row/column range selection
- **Boolean conditions** — `&`, `|` operators with parentheses (never Python `and`/`or`)
- **`df.query()`** — SQL-like string filtering for readable conditions
- **`df.nlargest()` / `df.nsmallest()`** — fast outlier/top-N detection
- **`pd.concat()`** — combining multiple DataFrames row-wise
- **CSV I/O** — `df.to_csv()` with `index=False`, `pd.read_csv()`
- **Data quality** — missing values, duplicates, memory, unique value analysis

---

*Assignment completed for Day 14 | AM session.*
